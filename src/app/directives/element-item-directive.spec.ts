import { ElementItemDirective } from './element-item-directive';

describe('ElementItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementItemDirective();
    expect(directive).toBeTruthy();
  });
});
