import { ImageVisibilityFilterPipePipe } from './image-visibility-filter-pipe-pipe';

describe('ImageVisibilityFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageVisibilityFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
