import { NavProfilePipePipe } from './nav-profile-pipe-pipe';

describe('NavProfilePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NavProfilePipePipe();
    expect(pipe).toBeTruthy();
  });
});
